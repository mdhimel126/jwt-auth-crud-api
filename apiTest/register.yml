info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/register
  body:
    type: json
    data: |-
      {
        "name":"tonmoy",
        "email":"tonmoy@gmail.com",
        "password":"tonmoy123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
