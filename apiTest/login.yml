info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5000/api/login
  body:
    type: json
    data: |-
      {
        "email":"tonmoy@gmail.com",
        "password":"tonmoy123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
